.wrapper
  .container
    .top-half
      .square-top-left.color(val = 1)
      .square-top-right.color(val = 2)
    .bottom-half
      .square-bottom-left.color(val = 3)
      .square-bottom-right.color(val = 4)
    .center
      .text Simon
      .screen-wrap
        .screen
          .count __
        .label COUNT
      .wrap
        .start-wrap
          .start
          .label START
        .strict-wrap
          .strict
          .label STRICT
      .onoff
        .left OFF
        .right ON
        .slider
